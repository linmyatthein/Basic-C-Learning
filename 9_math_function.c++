#include<iostream>
#include<cmath>

int main()
{
    float x=3.14 ,y=5 ,z;

   // z=std::max(x,y);
   // z=std::min(x,y);
   // z=pow(3,2);
   // z=sqrt(9);
   // z=abs(-5);
   // z=round(x);
   // z=ceil(x);
   // z=floor(x);
   
    std::cout<<z<<std::endl;
    
    return 0;
}